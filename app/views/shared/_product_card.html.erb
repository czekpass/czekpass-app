<div class="card-product custom-card">
  <div class="photo">
    <p><%= product.photo %></p>
  </div>
  <div class="card-product-infos">
    <h2><%= link_to product.name, business_product_path(@business, product) %>/h2>
    <p><%= humanized_money_with_symbol(product.price) %></p>
  </div>
