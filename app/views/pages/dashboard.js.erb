// 1. figure out what you want to do on search
// We want to change the total amount saved for that period.
// 2. select the element that updates and store in variable

  // 3. document.querySelector('#total-savings')

// 4. Change the innerText to be @total_saving

// 5. You can use JS and ERB in this file, check Kitt for example


function refreshTimedSavings(total_saving) {
  const newTimeForm = document.getElementById('total-timed-savings');
  newTimeForm.innerText = total_saving;
}

refreshTimedSavings('Your total savings for this time are: $<%= @total_saving %>');
