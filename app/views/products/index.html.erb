<section class="clean-block clean-catalog dark">
  <div class="container">
    <div class="block-heading">
      <h2 class="text-info">Business Products</h2>
    </div>

    <div class="content">
      <div class="products pl-5 card-grid-3c">
        <% @products.each do |product| %>
          <br>
          <%= link_to product.name, business_product_path(product.business, product) %>
        <% end %>
      </div>
    </div>
  </div>
</section>
